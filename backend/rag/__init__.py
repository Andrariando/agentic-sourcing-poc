"""RAG layer - Vector store and retrieval.

Import from submodules explicitly, e.g. `from backend.rag.retriever import DocumentRetriever`.
This package __init__ intentionally does not re-export submodules to avoid circular imports
with backend.infrastructure.storage_providers.
"""
